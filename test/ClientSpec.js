describe('Client!', function() {

  describe('ShortenLinkController', function() {
    beforeEach(angular.module('Shortly'));

    beforeEach(inject(function($injector){
      rootScope = $injector.get('$rootScope');
      $scope
      controller = $injector.get('ShowLinksController');

      makeController
    });
  });
});
